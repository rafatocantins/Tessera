{
  "name": "@secureclaw/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "secureclaw": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/bin.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@secureclaw/shared": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.7.0"
  }
}
